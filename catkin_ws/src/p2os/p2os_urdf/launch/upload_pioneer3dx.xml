<launch>
  <!-- send table.xml to param server -->
  <arg name="robot_name"/>
  <param name="robot_description" command="$(find xacro)/xacro.py '$(find p2os_urdf)/defs/pioneer3dx.xacro' prefix:=$(arg robot_name)" />
</launch>

